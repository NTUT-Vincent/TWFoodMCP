name: Scrape McDonald's Taiwan nutrition

on:
  workflow_dispatch:
  pull_request:
    paths:
      - scripts/scrape-mcdonalds-tw-api.mjs
      - .github/workflows/mcdonalds-scrape.yml

permissions:
  contents: read

jobs:
  scrape:
    runs-on: ubuntu-latest
    timeout-minutes: 30
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24

      - name: Crawl official McDonald's Taiwan nutrition API
        run: |
          mkdir -p artifacts
          node scripts/scrape-mcdonalds-tw-api.mjs \
            --output artifacts/mcdonalds-tw-nutrition.api.raw.json

      - name: Upload raw snapshot for review
        uses: actions/upload-artifact@v4
        with:
          name: mcdonalds-tw-nutrition-api-raw
          path: artifacts/mcdonalds-tw-nutrition.api.raw.json
          if-no-files-found: error
          retention-days: 14
